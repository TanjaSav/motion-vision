import PageTransitionsSection from "@/components/Transitions/PageTransitionsSection";

// Page transitions route. The interactive section is split into components/Transitions.
export default function PageTransitions() {
  return <PageTransitionsSection />;
}